package main.java.dio.pm.praticando.domain;

public non-sealed class HalfTicket extends Ticket{

    public HalfTicket(Double value, String name, String movieDetail) {
        super(value, name, movieDetail);
    }
}
